// Thin re-export. The named-profile registry lives in @deepthonk/providers so MCP can share it.
export { listProfiles, loadNamedProfile, profilePath, profilesDir, NAMED_PROFILE_NAME_RE } from "@deepthonk/providers";
export type { NamedProfileBundle } from "@deepthonk/providers";
